# Copy this file to secrets.toml and fill in real values.
# secrets.toml is gitignored — never commit it.
# On Streamlit Cloud, enter these values in the app's Secrets dashboard instead.

[users]
birgitte = "choose_a_password"
monica   = "choose_a_password"

[aws]
bearer_token = "your_aws_bedrock_bearer_token"
